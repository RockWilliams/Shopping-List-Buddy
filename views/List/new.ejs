<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/styles/main.css" type="text/css">
    <title>New Shopping List</title>
    <script
  src="https://code.jquery.com/jquery-3.5.1.min.js"
  integrity="sha256-9/aliU8dGd2tb6OSsuzixeV4y/faTqgFtohetphbbj0="
  crossorigin="anonymous" defer></script>
    <script src="/scripts/app.js" defer></script>
    <link rel="stylesheet"  href="/styles/main.css" type="text/css">
</head>
<body>
    <div>
        <h1>New Shopping List for <%= stores.name %> </h1>
        <form action="/store/<%= stores._id %>" method="POST">
            <input type="text" name="name" placeholder="name">
            <p>
                <select name="products" multiple>
                    <% stores.products.forEach(product => { %>
                        <option class="price" value="<%= product._id %>"> <%= product.name %> </option>
                    <% }) %>
                </select>
            </p>
            <input type="submit" value="Add to List!">
        </form>
    </div>
    <div id="list-div"><ul id="list"></ul></div>
</body>
</html>
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <h1>Stores</h1>
    <h2>Create Store</h2>
    <a href="/store/new">Add a Store</a>
    <select name="stores">
    <% stores.forEach(stores => { %>
         <option value="<%= stores._id %>"><%= stores.name %></option>
    <% }) %>
    </select>
   
</body>
</html>